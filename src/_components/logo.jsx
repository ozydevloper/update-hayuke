import { Button } from "@/components/ui/button";

const Logo = () => {
  return (
    <Button
      variant={"ghost"}
      className="font-bold text-xl text-center text-primary"
    >
      Hayuke
    </Button>
  );
};
export default Logo;
